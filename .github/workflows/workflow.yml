name: CI
on:
    push:
        branches: master
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Branch name
              run: echo running on branch ${GITHUB_REF##*/}
            - name: Check branch name
			        if: contains(${GITHUB_REF##*/}, 'test')
              run: exho 'branch name contains test'
